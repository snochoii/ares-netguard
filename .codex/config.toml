# Project-scoped Codex configuration for ARES NetGuard-ML.
# Keep agent definitions in .codex/agents/*.toml and repo skills in .agents/skills/*/SKILL.md.

[agents]
max_concurrent_threads_per_session = 3
